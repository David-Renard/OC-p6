{% if comments.pages > 1 %}
<ul class="pagination">
    {% if comments.page < comments.pages %}
    <li class="page-item m-auto w-100 mt-3">
        <a href="{{ path("show_trick", {slug : trick.slug, page : comments.page + 1}) }}#comments" class="btn btn-secondary">Voir plus de commentaires</a>
    </li>
    {% endif %}
</ul>
{% if comments.page > 1 %}
<a href="{{ path("show_trick", {slug : trick.slug, page : 1}) }}#comments" class="btn btn-light">Voir moins de commentaires</a>
{% endif %}
{% endif %}