{% extends 'base.html.twig' %}

{% block title %}Ajouter un trick{% endblock %}

{% block body %}
<div class="container register text-center text-primary pt-3 bg-secondary-subtle rounded-2 my-3 my-sm-5">
    <div class="row h3">
        <h1 class="h3">Ajoute une figure et quelques médias !</h1>
    </div>
    {{ form_errors(addTrickForm) }}
    {{ form_start(addTrickForm) }}
    <div class="my-3 d-flex justify-content-around">
        {{ form_row(addTrickForm.name) }}
    </div>
    <div class="my-3 d-flex justify-content-around">
        {{ form_row(addTrickForm.description) }}
    </div>
    <div class="mt-3 w-50 m-auto d-flex flex-column flex-sm-row justify-content-around align-items-center">
        <div>
            {{ form_row(addTrickForm.category) }}
        </div>
        <div>
            {{ form_row(addTrickForm.newCategoryName) }}
        </div>
    </div>
    <div class="mt-3 d-none justify-content-around">
        {{ form_row(addTrickForm.author) }}
    </div>
    {{ form_end(addTrickForm) }}
</div>
{% endblock %}
